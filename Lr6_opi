   else {
        if ((c >= 5) && (a != 0)) {
            F = (a - x) / (c * x);
        }
        else {
            F = (2 * x) / (c - 5);
        }
        cout << setw(8) << setprecision(8) << "F=" << F << endl;;

    }
}
